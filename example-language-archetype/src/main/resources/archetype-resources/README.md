# ${artifactId}

Proyecto generado desde `LexCupStudio` para iniciar un lenguaje con JFlex + CUP.

## Build

```bash
mvn clean test
```

## Qué incluye

- `src/main/jflex/example.flex`
- `src/main/cup/example.cup`
- adapters (`ExampleLexerAdapter`, `ExampleParserAdapter`)
- test de integración con `LanguageFrontend`
